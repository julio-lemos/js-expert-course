{
  "name": "js-expert-course",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "projects/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev --parallel",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "prepare": "husky install"
  },
  "devDependencies": {
    "husky": "^8.0.0",
    "prettier": "latest",
    "pretty-quick": "^3.1.3",
    "turbo": "latest"
  },
  "engines": {
    "yarn": ">= 1.0.0",
    "node": ">=16.0.0",
    "npm": "Você deve usar yarn nesse projeto"
  },
  "dependencies": {},
  "packageManager": "yarn@1.22.18"
}
